import com.squareup.okhttp.OkHttpClient;
import com.squareup.okhttp.Request;
import com.squareup.okhttp.Response;
import io.cucumber.java.en.When;

import java.net.URI;

public class FilmRestStep {
/*    private OkHttpClient client=new OkHttpClient();
    private Response response;

    @When("The user sends GET requeest to the endpoint")
    public void theUserSendsGETRequestToTheEndpoint(){
        try {
            Request request=new Request.Builder()
                    .url(URI.create("https://online-movie-database.p.rapidapi.com/title/get-images?tconst="+id))
                    .header("rapidapi-host", "online-movie-database.p.rapidapi.com")
                    .header("rapidapi-key", "337e15bfb1msh732ba30e394f6f8p17a6e5jsn9be3c0c88054")
                    .build();
        }catch ()
    }*/
}
